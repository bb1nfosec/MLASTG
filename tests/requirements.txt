# MLASTG Test Script Dependencies
# Core ML security testing (required)
adversarial-robustness-toolbox>=1.16
numpy>=1.24
scipy>=1.10
scikit-learn>=1.2

# Deep learning frameworks (choose one based on your stack)
torch>=2.0
torchvision>=0.15

# LLM security testing
giskard>=2.0

# Privacy testing
diffprivlib>=0.6
aif360>=0.5

# NLP adversarial testing
textattack>=0.3

# Supply chain
modelscan>=0.1

# Reporting
pytest>=7.0

# Optional: advanced ML security (may require additional setup)
# secml>=0.14            # ML security evaluation (may need system deps)
# rebuff>=0.2            # Prompt injection detection
# promptinject>=0.1      # Prompt injection framework
# privacy-metrics>=0.1   # Privacy risk measurement
# pytest-json>=0.4       # JSON test reporting
